<div class="ham-menu--wrapper" x-show="hamMenuOpen">
  <div class="bg-blk-opacity-05" x-on:click="hamMenuOpen = false"></div>
  <div class="ham-menu">
    <div class="ham-menu__icon-close" x-on:click="hamMenuOpen = false">
      {% render 'icon-ham-menu-close' %}
    </div>
    <div class="ham-menu__menu-links">
      <!-- Main menu inside ham-menu -->
      {% for link in linklists.main-menu.links -%}
        <div class="ham-menu__menu-links--every-link">
          {{ link.title | link_to: link.url }}
          <div class="ham-menu__menu-links--chevron-down">
            {% render 'icon-chevron-down' %}
          </div>
        </div>
      {%- endfor %}
    </div>
    <div class="ham-menu__footer-links">
      <!-- Footer menu inside ham-menu -->
      {% for link in linklists.footer.links -%}
        {{ link.title | link_to: link.url }}
      {%- endfor %}
      {% for link in linklists.footer-second-menu.links limit: 2 -%}
        {{ link.title | link_to: link.url }}
      {%- endfor %}
    </div>
  </div>
</div>